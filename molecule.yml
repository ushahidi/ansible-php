---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-php
      image: ubuntu
      image_version: trusty
      ansible_groups:
        - group1
verifier:
  name: testinfra
